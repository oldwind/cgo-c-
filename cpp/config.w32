// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("goproxy", "for goproxy support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("goproxy", "enable goproxy support", "no");

if (PHP_GOPROXY != "no") {
	EXTENSION("goproxy", "goproxy.c");
}

